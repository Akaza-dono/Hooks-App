import { render, screen } from "@testing-library/react";

describe('Test about Multiple custom hooks', () => { 
    test('should show the componene by default', () => { 
        //render(<MultipleCustomHokks/>);
        // screen.debug();
     });
 })